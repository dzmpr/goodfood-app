<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.GoodFood" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/emerald</item>
        <item name="colorPrimaryVariant">@color/spring_green</item>
        <item name="colorOnPrimary">@color/ivory</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/medium_spring_green</item>
        <item name="colorSecondaryVariant">@color/mint_green</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorSurface</item>
        <!-- Error colors. -->
        <item name="colorError">@color/rose_madder</item>
        <item name="colorOnError">@color/ivory</item>
        <!-- Surface color. -->
        <item name="colorOnSurface">@color/white</item>
        <!-- Customize your theme here. -->
        <item name="bottomSheetDialogTheme">@style/BottomSheetDialogStyle</item>
    </style>

    <style name="Theme.GoodFood.ActionBar" parent="ThemeOverlay.MaterialComponents.Dark.ActionBar">
        <item name="iconTint">?attr/colorOnSurface</item>
        <item name="colorControlNormal">?attr/colorOnSurface</item>
        <item name="tint">?attr/colorOnSurface</item>
        <item name="android:background">?attr/colorSurface</item>
    </style>

    <style name="BottomSheetDialogStyle" parent="ThemeOverlay.MaterialComponents.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/BottomSheetStyle</item>
        <!-- Prevent keyboard to draw above BottomSheetDialog -->
        <item name="android:windowSoftInputMode">adjustResize</item>
        <item name="android:windowIsFloating">false</item>
    </style>

    <style name="BottomSheetStyle" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@drawable/bg_bottom_sheet</item>
    </style>

    <style name="Theme.GoodFood.FilterChip" parent="Widget.MaterialComponents.Chip.Filter">
        <item name="chipBackgroundColor">@color/chip_background_color</item>
        <item name="checkedIconTint">@color/white</item>
    </style>
</resources>