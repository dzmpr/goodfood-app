<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Cooked" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/emerald</item>
        <item name="colorPrimaryVariant">@color/spring_green</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/medium_spring_green</item>
        <item name="colorSecondaryVariant">@color/spring_green</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorSurface</item>
        <!-- Error colors. -->
        <item name="colorError">@color/rose_madder</item>
        <item name="colorOnError">@color/white</item>
        <!-- Customize your theme here. -->
        <item name="colorSurface">@color/white</item>
        <item name="colorOnSurface">@color/black</item>
        <!-- Light theme for status bar. -->
        <item name="android:windowLightStatusBar">true</item>

        <item name="bottomSheetDialogTheme">@style/BottomSheetDialogStyle</item>
        <item name="alertDialogTheme">@style/Theme.Cooked.Dialog</item>
        <item name="android:alertDialogTheme">@style/Theme.Cooked.AlertDialog</item>
    </style>

    <style name="Theme.Cooked.Splashscreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/ic_launcher_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.Cooked</item>
        <item name="android:windowLayoutInDisplayCutoutMode" tools:targetApi="o_mr1">default</item>
    </style>
</resources>
